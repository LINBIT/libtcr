override CFLAGS += @CFLAGS@
ARCH	:= $(shell uname -m | sed -e s/i.86/i386/)

.PHONY: clean

all: libtcr.a

clean:
	rm -f *.o *.a

libtcr.a: threaded_cr.o coroutines.o swapcontext_fast_$(ARCH).o
	ar rcs $@ $^

dep:
	$(CC) -MM $(wildcard *.c) > Makefile.deps

-include Makefile.deps
